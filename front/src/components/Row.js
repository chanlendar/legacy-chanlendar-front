import React from "react";

/**
 * This component is just for pseudo-design
 * it will be removed soon.
 */
function Row({ children }) {
	return (
        <div>
            가상의 Row
            {children}
        </div>
    );
}

export default Row;
